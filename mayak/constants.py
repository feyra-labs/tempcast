"""Глобальные константы архитектуры МАЯК"""
import torch

H = 168 # горизонт прогноза, ч
L_MAX = 672 # максимальная длина истории, ч

M = 24 # количество мод
GROUPS = (8, 6, 4, 6) # размеры групп R / D / S / W (сумма = M)

DZ = 16 # размер паспорта станции z
C_ENC = 48 # число каналов синоптического энкодера
N_CH = 13 # число входных каналов энкодера

QUANTILES = (0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95)
NQ = len(QUANTILES)


def inv_softplus(y: torch.Tensor) -> torch.Tensor:
    """x такой, что softplus(x) = y"""
    return torch.log(torch.expm1(y))